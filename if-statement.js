var fruit = 'orange'; //creating the variable "fruit" and assigning it the string "orange"

var fruit = fruit.length; //taking the string we assigned and returning the length

if (fruit > 5) {
  console.log('The fruit name has more than five characters.');
}
else {
  console.log('The fruit name has five characters or less.');
}